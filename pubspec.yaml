name: easy_compass
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sensors_plus:
  mockito:


dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter
